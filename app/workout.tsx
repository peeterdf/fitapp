import { SafeAreaView } from 'react-native-safe-area-context';
import WorkoutScreen from '../src/screens/WorkoutScreen';
export default function Page() { return <SafeAreaView style={{ flex: 1, backgroundColor: '#0f0f0f' }}><WorkoutScreen /></SafeAreaView>; }
