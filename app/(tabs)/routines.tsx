import { SafeAreaView } from 'react-native-safe-area-context';
import RoutinesScreen from '../../src/screens/RoutinesScreen';
export default function Tab() { return <SafeAreaView style={{ flex: 1, backgroundColor: '#0f0f0f' }}><RoutinesScreen /></SafeAreaView>; }
