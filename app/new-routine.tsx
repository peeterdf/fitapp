import { SafeAreaView } from 'react-native-safe-area-context';
import NewRoutineScreen from '../src/screens/NewRoutineScreen';
export default function Page() { return <SafeAreaView style={{ flex: 1, backgroundColor: '#0f0f0f' }}><NewRoutineScreen /></SafeAreaView>; }
